import React from "react";
import ReactDOM from "react-dom";

import Bar from "./App";

const rootElement = document.getElementById("root");
ReactDOM.render(
  <React.StrictMode>
    <Bar />
  </React.StrictMode>,
  rootElement
);
